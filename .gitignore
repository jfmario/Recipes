# Python virtual environment (use one project venv; activate before pip/scripts)
.venv/
venv/

output/*

# macOS
.DS_Store

# Windows
Thumbs.db

# Claude Code — local overrides (not shared)
.claude/settings.local.json
