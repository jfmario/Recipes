name: TST Build

on:
  push:
    branches:
      - release/**
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/build-release.yml
    with:
      env-key: TST
      prerelease: true
      include-individual-pdfs: false
      release-prefix: cookbook
