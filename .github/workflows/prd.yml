name: PRD Build

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/build-release.yml
    with:
      env-key: PRD
      prerelease: false
      include-individual-pdfs: false
      release-prefix: cookbook
